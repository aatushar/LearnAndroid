name: combined_project
description: A comprehensive Flutter app combining various features.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  google_maps_flutter: ^2.3.0
  geolocator: ^9.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
